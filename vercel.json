{
  "buildCommand": "cd web_frontend && npm install && npm run build",
  "outputDirectory": "web_frontend/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
