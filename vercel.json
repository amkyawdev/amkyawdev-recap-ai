{
  "builds": [
    {
      "src": "web_frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "web_frontend/dist"
      }
    }
  ],
  "routes": [
    {"src": "/(.*)", "dest": "/web_frontend/dist/$1"}
  ]
}
